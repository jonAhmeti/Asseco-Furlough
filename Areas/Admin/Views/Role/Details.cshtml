@model Furlough.DAL.Models.Role

@{
    ViewData["Title"] = "Details";
}


<div class="container justify-content-center">
    <div class="w-100 d-flex justify-content-center">
        <div class="blue-gradient customSubPageTitle d-flex justify-content-center align-middle">
            <h2 class="text-white mt-1">Role</h2>
        </div>
    </div>
    <div class="w-100 d-flex justify-content-center">
        <div class="bg-danger d-flex justify-content-center">
            <div>
                <h3>Details</h3>
            </div>
            <div>
                <dl class="row">
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.Title)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.Title)
                    </dd>
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.Description)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.Description)
                    </dd>
                </dl>
            </div>
        </div>
        <div class="">
            <a asp-action="Edit" asp-route-id="@Model.Id">@Resources.Shared._Admin_Layout_en.edit</a> |
            <a asp-action="Index">@Resources.Shared._Admin_Layout_en.backToList</a>
        </div>
    </div>
</div>